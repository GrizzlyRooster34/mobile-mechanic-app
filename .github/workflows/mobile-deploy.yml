name: Mobile App Deployment

on:
  push:
    branches: [main, expo-mobile-deploy]
    paths:
      - 'app.config.js'
      - 'package.json'
      - 'src/**'
      - 'assets/**'
  pull_request:
    branches: [main]
    paths:
      - 'app.config.js'
      - 'package.json'
      - 'src/**'
      - 'assets/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Setup Expo CLI
        run: npm install -g @expo/cli eas-cli
        
      - name: Install dependencies
        run: npm ci
        
      - name: Generate Prisma client
        run: npx prisma generate
        
      - name: Lint code
        run: npm run lint
        
      - name: Expo Doctor
        run: expo doctor
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_ACCESS_TOKEN }}
          
      - name: Build for Preview (Development)
        if: github.event_name == 'pull_request'
        run: eas build --platform all --profile preview --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_ACCESS_TOKEN }}
          
      - name: Build for Production
        if: github.ref == 'refs/heads/main'
        run: eas build --platform all --profile production --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_ACCESS_TOKEN }}
          
      - name: Create Update (OTA)
        if: github.ref == 'refs/heads/main'
        run: eas update --branch production --message "${{ github.event.head_commit.message }}"
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_ACCESS_TOKEN }}
          
      - name: Comment PR with Preview
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🚀 Mobile app preview build completed! Check your Expo dashboard for the latest build.'
            })